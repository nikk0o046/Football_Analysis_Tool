# Make sure your DATABASE_URL:
# - Starts with: postgresql+asyncpg://       (NOT just postgresql://)
# - Does NOT include: ?sslmode=require       (SSL is set via connect_args instead)

# Example:
# Wrong:  postgresql://user:pass@host/dbname?sslmode=require
# Right:  postgresql+asyncpg://user:pass@host/dbname

DATABASE_URL=  # Get this from Neon and modify as instructed above
SUPABASE_JWT_SECRET=  # Get this from Supabase >> Project >> API
